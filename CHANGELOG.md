# 更新日志

本项目的用户可见改动都记录在这里。**1.x 起为正式版本**（此前的 0.x 为测试/预发布阶段）。
桌面打包后，本文件会自动复制到 `packages/desktop/release/`，与 exe 放在一起。

## [1.3.1] - 2026-09-24

### 新增

- **终端改为底部 dock**：侧栏「终端」按钮（移到了「设置」上方）可升起/收起，位于内容区内不遮挡侧栏与顶栏；**可拖拽调整高度**（记忆在偏好里）；收起后终端会话不中断。
- **里程碑页**（侧栏新入口，奖杯图标）：内含 **学习热力图**（最近 12 周 + 连续/最长天数）、**学习曲线**（近 16 周累计学习天数折线）、**成就**。
- **成就系统**：23 个带独特名字的成就（开卷有益 / 笔耕不辍 / 三日之约 / 炉火纯青 / 更上层楼 / 问道于师 / 工欲善其事 …），点击可展开看任务与进度；达成时右下角弹出「解锁成就」提示（去重、首次运行静默登记基线、记录达成时间）。
- **能力地图四维进度条**：每个 G 用「掌握 / 投入 / 持续 / 热度」四条进度条替代原来的百分比。

### 变更

- 概览的「里程碑」卡改名为「成就」，热力图与学习曲线移入「里程碑」页。
- **Tooltip 统一**：全站 hover 提示统一走应用自绘样式（原来部分用浏览器原生 `title`）；侧栏功能区在展开（有文字）时不显示悬停提示，仅收起成图标栏时显示。
- 对话流式输出改为按帧合并渲染，长会话更顺滑。

### 修复

- 修 `Modal` 每次输入都抢焦点（`onClose` 身份变化导致聚焦 effect 重跑），生成目标/重命名会话输入框不再跳焦。
- 修能力地图卡片头被按钮挤到换行、详情框与 G 行不同宽；去掉多余的「收起」按钮（再点一次 G 即收起）。

## [1.3.0] - 2026-09-24

### 新增

- **首启向导 + 概览引导**：首次运行改为多步向导（选学习库 → 配模型 → **用模型生成学习目标** → 就绪）；概览「开始使用」清单；概览「写今天」一键生成当天 `SUMMARY.md` 并跳转。
- **用模型生成 GOALS.md**：在「能力地图」卡片点「生成 / 更新目标…」，用自然语言写目标，由你接入的模型整理成符合格式的 `GOALS.md`（生成前校验、格式不对自动带反馈重试一次、保存前可编辑预览；也接入了首启向导与未初始化空状态）。
- **外部插件系统**：应用数据目录 `plugins/` 下每个子目录一个插件（`plugin.json` + 入口），可贡献**命令面板命令 / agent 工具 / 后端 handler**；设置 → 插件 可查看已安装与加载错误。开发文档见 `docs/PLUGINS.md`。
- **写入备份**：覆盖写 `PROGRESS.md` / `GOALS.md` / `SUMMARY.md` 等之前，自动备份到工作区 `.myblog/backups/`（每个文件最多留 20 份，并写 `.gitignore`）。
- **能力地图真实活跃度**：按学习记录显示每个 G 的「出现天数 / 最近日期」。
- **对话上下文用量**：输入框上方显示上下文 token 估算与进度条（≥85% 提示并给「新建会话」；窗口大小可在 设置 → 功能 配置）。
- **对话增强**：每条回答显示耗时；聊天头「引用总结」把某天 `SUMMARY.md` 以引用块插入输入框。
- **每日总结自动保存**：停止输入约 1.2s 自动落盘并显示「已保存 hh:mm」；切换日期前先落盘；有未保存时离开页面会拦截提醒。
- **设置 → 插件**（拼图图标）：个性签名——顶栏标题旁可开关的一句话。
- **命令面板**新增动作：写今天总结、生成 / 更新学习目标、切换工作区、开 / 关个性签名；插件命令也会出现在命令面板。
- **文件视图**：Markdown 相对链接点击可在工作区内跳转；图片点击放大（Esc / 点击关闭）。
- 统一 `Modal`（Esc 关闭、Tab 焦点循环、点遮罩关闭）与 `EmptyState` 组件。

### 变更

- **每日总结文件名默认由 `总结.md` 改为 `SUMMARY.md`**；打开旧工作区时会自动把 `YYYY-MM-DD/总结.md` 改名为 `SUMMARY.md` 并同步 `PROGRESS.md` 里的链接（迁移前先备份）。
- **有进展才生成**：agent 当天没有实际进展时不建日期目录 / 当日总结、不写 `PROGRESS.md`（提示词与模板均已更新）。
- agent 规划默认低温度（0.3）；注入上下文精简为「活跃能力 + 最近 5 条记录」；规划输出限制「最多 3 条、每条一行」。
- 「岗位目标」文案统一为「学习目标」。
- 对话设置改为弹窗（点框外 / Esc 关闭），高级项默认折叠；移除与服务商预设重复的「从 opencode 导入」。

### 修复

- 修 `EPERM: operation not permitted, mkdir 'D:\'`：存储路径校验（模型配置必须是文件、历史必须是目录、拒绝盘符根）+ 写前判断目录并容忍 `EPERM`。
- `parseGoals` 容忍标题别名（`当前阶段/阶段`、`能力编号/能力清单`、`阶段顺序/学习路径`），避免生成或手写的地图被静默解析成空。
- 对话设置里 OpenCode 预设的默认模型改为该网关可用的 `deepseek-v4-flash` / `glm-4.7`。

## [1.2.3] - 2026-09-22

### 新增

- **对话历史目录可读化 + 可迁移**：历史目录从纯哈希（`3c7afcafa7673ad1`）改成 **`<别名或文件夹名>-<短哈希>`**（如 `手机逆向-3c7afcaf`），并在目录里写入 `workspace.json`（记录原始路径、别名、更新时间）。旧哈希目录首次访问时自动改名迁移；**改了工作区别名也会自动改名目录并保住会话**；最早的 `<哈希>.json` 单文件历史也会自动搬进新结构。
- **Anthropic 格式支持**：`packages/agent` 新增 Anthropic Messages provider（`/messages`、`x-api-key`、`anthropic-version`、SSE `content_block_delta` / `tool_use` 解析、`tool_result` 合并）。对话设置里新增「API 格式：自动 / OpenAI 兼容 / Anthropic」，默认按 baseURL 与模型名推断（`claude*` → Anthropic）。
- **opencode 网关修复与模型选择**：「导入 opencode」现在会读 `~/.cache/opencode/models.json` **列出该网关的模型**（标注 Anthropic / 格式不支持），可指定模型导入并自动带上正确的格式；对 opencode 网关的请求会**自动补 `x-opencode-session` 头**（Go 网关没有这个头会 400）。
- **每个工作区可以单独指定模型**（对话 → 设置）：
  - 「保存到」：默认（全局配置）/ 命名**配置档** / **工作区文件** `myblog.agent.json` / ＋新建配置档；
  - 「此工作区使用」：继承默认，或绑定某个配置档；
  - **生效优先级**：环境变量 `MYBLOG_AGENT_*` → 工作区里的 `myblog.agent.json` → 绑定的配置档 → 默认配置；设置面板顶部显示「当前生效」来源。
  - 配置档存在 `settings.json` 的 `profiles`，绑定关系在 `workspaceProfiles`；删配置档会同时解除绑定。

### 修复

- **修 `EPERM: operation not permitted, mkdir 'D:\'`**：根因是「设置 → 存储」里的模型配置被填成了**目录**（如 `D:\Work`），写配置时 `mkdir(dirname(...))` 变成 `mkdir('D:\')`。现在：
  - 保存时校验：模型配置必须是**文件**路径（拒绝目录、拒绝盘符根目录），历史目录必须是**目录**（拒绝文件、拒绝盘符根目录），并且报错信息直接说清该怎么填；
  - 写模型配置、初始化工作区时先判断目录是否存在，容忍 `EPERM/EACCES`，盘符根不会再炸。
- 「设置 → 存储」两处说明补上「要填文件还是目录」的例子。

## [1.2.2] - 2026-09-22

### 新增

- **工作区管理**：侧栏工作区下拉新增「管理工作区…」，可以
  - **重命名**（给工作区起别名，侧栏与下拉都显示别名，留空则恢复文件夹名）；
  - **从列表移除**（只取消登记，**不会删除磁盘文件**；移除当前工作区会自动切到剩下的一个；至少保留一个）。
  - 别名与工作区列表一起存在 `settings.json` 的 `names` 字段（旧配置无该字段也能正常读）。
  - 对应的 handler：`renameWorkspace` / `removeWorkspace`，`workspaces()` 返回值新增 `names`。

## [1.2.1] - 2026-09-22

### 新增

- **应用图标**：`packages/desktop/build/` 下新增品牌图标（品牌色渐变圆角 + 白色 M，与界面左上角一致），由 `scripts/make-icons.mjs` 生成（512 PNG + 7 尺寸 ICO + NSIS 侧栏/头部 BMP）。exe、任务栏、安装程序不再使用 Electron 默认图标。
- **安装程序 UI 定制**（NSIS）：
  - 向导界面为**简体中文**（`language: 2052`），带**许可协议页**（MIT）。
  - 安装程序 / 卸载程序 / 向导头部图标换成 MyBlog 图标；左右页用品牌侧栏图（164×314）与头部图（150×57）。
  - 每用户安装（不需要管理员），可选安装目录，建桌面与开始菜单快捷方式（快捷方式名 `MyBlog`，开始菜单不建分类目录），安装完成可直接运行。

### 变更

- 桌面端打包产物命名规范：`MyBlog_v<版本>_setup.exe`（安装版）/ `MyBlog_v<版本>_portable.exe`（免安装），并随 Release 附 `SHA256SUMS.txt`。

## [1.2.0] - 2026-09-22

### 新增

- **文件管理**（侧栏「每日总结」下面）：浏览当前工作区的目录树，点文件即可预览内容（目录优先排序、面包屑导航、上一级、显示大小、复制内容、超过 256KB 提示截断、二进制文件提示不支持预览）；`.md` 文件默认**渲染 Markdown 预览**，可切「预览 / 源码」；`.git` / `node_modules` / `dist` / `release` 自动隐藏，路径限制在工作区内；开启实时同步时文件变化会自动刷新。对应的 handler 是 `files` / `readFile`，与 agent 的 `fs_list` / `fs_read` 共用同一套沙箱。
  - 左右两栏高度固定、各自内部滚动，切换文件时列表长度不再变化；长文件名用省略号截断，不挤压标题栏；预览卡片不再显示文件大小（大小只在左侧列表里显示）。
- **对话**：「重新生成 / 复制」按钮移到**气泡下方**（仍然 hover 才显示），不再挤在气泡右边。
- **每日总结**：「分栏」模式下编辑区与预览**滚动同步**，两栏等高（62vh）。
- 侧栏**工作区切换图标**换成小扳手（与「设置」区分开）。
- **强调色扩充到 8 种**：蓝 / 紫 / 绿 / 橙 / 玫红 / 青 / 琥珀 / 青柠（深浅主题各有对应色）。

### 变更

- **MCP 新增文件工具**：`myblog mcp` 现在也暴露 `fs_list` / `fs_read` / `fs_write`，外部 AI 客户端（opencode / Claude / Cursor）可以直接读写工作区里的普通文件（脚本、代码、笔记），与内置对话**共用同一套沙箱**（路径限制在工作区内、写入默认 `dryRun=true` 先出 diff）。
- 概览的**能力地图改为紧凑单行**（G 编号 + 能力 + 进度条/百分比 + 状态），行高从 ~104px 降到 ~36px，**12 个 G 一屏完整显示、不需要横向或纵向滑动**。点某个 G 时详情**就地展开在那个 G 下面**（只保留：编号+名称、收起、验证问题、当前状态输入+保存、快捷状态），不再在卡片底部单独开一块，也不再列「相关总结」；**时间线自动按该 G 筛选**，取消选中即恢复完整流水。卡片按内容高度排列（不被网格行拉伸），行内用网格列 + `minmax(0,1fr)` 排版并禁止横向滚动；只有超过 12 个能力时才出现纵向滚动与「展开全部 N / 收起」。
- 统一**自定义悬停提示**：只给**图标按钮 / 色点**这类没有可见文字的必要位置（对话里的重命名/删除、窗口按钮、日历翻页、文件视图刷新/复制、强调色点、命令面板入口）换成应用内 tooltip，样式跟随主题与深浅色；有文字标签的地方（侧栏功能列表、工作区名、文件路径等）仍用系统原生提示。修掉了 tooltip 位置偏移：`#root` 的 `zoom`（字号偏好）会把子树内 fixed 坐标乘系数，改为 portal 到 `body` 后实测 X/Y 精确对齐。
- **工作区文件名改为 `PROGRESS.md` + `GOALS.md`**（原 `学习进度总览.md` / `岗位目标.md`）：只认新文件名，旧名不再兼容；`myblog.config.json` 里可以任意改名。已有工作区需要把文件重命名（本机 `D:\Mobile`、`D:\Work\Test_1`、`D:\Work\Test_2` 已改好，agent 模板里的引用也一并更新）。
- **`GOALS.md` 变为可选**：没有它也能正常用（阶段与能力地图为空），校验只给一条**警告**（原来报红色错误）；概览的能力地图会显示空态与「生成 GOALS.md」入口。
- 修复「每日总结」**分栏联动在切换日期后失效**：编辑区/预览在读取过程中会被卸载重建，而滚动监听的 effect 只依赖模式，重建后不会重挂，于是必须再点一次「预览 / 分栏」才恢复。改用 callback ref 绑定元素，元素一重建就重新挂监听，实测切日期后双向联动仍然生效。
- 修复 `initWorkspace` 在根目录不存在时写文件失败（现在会先建目录）。
- **对话兼容 opencode**：服务商预设新增 **OpenCode Go**（`https://opencode.ai/zen/go/v1`，默认 `grok-4.6`）与 **OpenCode Zen**（`https://opencode.ai/zen/v1`）；本机 opencode 已登录时，对话设置里会检测 `~/.local/share/opencode/auth.json` 并给出「导入 OpenCode Go / Zen」按钮，一键把 baseURL / 模型 / key 写进 MyBlog 的 `agent.json`（key 不经过界面）。
- **能力地图可交互**：点某个 G **就地展开**（手风琴），可看验证问题、**直接改状态**（写回 `GOALS.md` 的状态单元格，外科手术式替换、保持行尾）、用快捷状态一键切换；选中时时间线自动按该 G 筛选。
- **全局内容搜索**：命令面板 `Ctrl+K` 输入 ≥2 个字符会搜索 `PROGRESS.md`、`GOALS.md` 与各天 `总结.md`，结果显示所在文件/日期与行号，回车跳到那天。
- **文件视图增强**：图片（png / jpg / gif / webp / svg / bmp…）直接预览（≤8MB 内嵌显示）；代码文件（java / ts / py / sh / json …）**语法高亮**并可在「预览 / 源码」间切换；**右键菜单**支持「复制相对路径 / 复制完整路径 / 在资源管理器中显示」。
- 概览新增**「版本」卡片**：显示当前分支、未提交改动数、最近一次提交；可**一键提交学习仓**（`git add -A`，提交信息默认按最新记录生成如 `close 2026-09-17 G2`，可改）。
- **大工作区性能**：每日总结的日期列表与概览时间线改为**分页**（各 120 条，点「显示更早的…」继续加载），不再一次性渲染全部记录。
- **通信层重构：不再绑定本机端口**。桌面端原先会在 `127.0.0.1` 上起一个随机端口的 HTTP 服务（Hono + SSE）来给界面喂数据；现在改为**纯 IPC**：渲染进程通过 preload 暴露的桥（`window.myblog.api`）直接调用主进程里与传输无关的 handler。
  - `@myblog/server` 去掉了 Hono / `@hono/node-server`，只保留 `createApi()` 这一层 handler（`dispatch` / `streamChat` / `watchChanges`）。
  - 桌面端删除了 `startServer`、端口分配与 `loadURL`，改为 `loadFile`；对话流式改为 `myblog:chat-event` 推送（可 `cancelChat` 中止），文件实时同步改为 `myblog:fs-change` 事件。
  - 顺带清掉了浏览器时代的残留：`window.prompt` 要 token 的逻辑、`localStorage` token、`/api` 开发代理。
  - 现在同机其他程序**无法再通过端口访问** MyBlog（此前服务没有任何鉴权）。
- **对话改成“普通对话优先”**：系统提示词明确「默认直接回答，只有用户明确要求读/写文件、记录进度、生成总结、校验时才调用工具」，并且不再每次回复都追问「需要我写入吗」；只是让你贴出文件内容时，直接写在回复里，不再自动调 `fs_write`。

## [1.1.3] - 2026-09-21

### 新增

- **主题选择改为图标**：跟随系统 = 半黑半白圆、深色 = 月亮、浅色 = 太阳（替换掉看着别扭的贴图）。
- **强调色**：蓝 / 紫 / 绿 / 橙可选。
- **字号**：小 / 中 / 大三档。
- **侧栏可折叠**为图标条；副标题显示当前工作区名。
- **命令面板 `Ctrl/Cmd+K`**：切页面、跳日期、刷新、切主题/风格、收展侧栏。
- **快捷键帮助 `?`**。
- **概览 hero 区**：工作区名 + 日期 + 阶段徽章 + 今日要做什么。
- **能力地图**：进度条旁显示百分比。
- **时间线**：按月份分组，记录显示 G 标签。
- **每日总结**：编辑 / 分栏 / 预览三种模式；日期列表按月分组。
- **对话**：重新生成、编辑并重发；工具调用合并为一个可展开分组；写回 diff 红绿高亮；导出对话为 Markdown（`.md`）。
- Toast 增加类型图标，最多堆叠 4 条；「新对话」加二次确认。
- **多工作区**：注册表 + 顶栏切换下拉；桌面端「添加工作区」（原生选目录）；切换仅限白名单，`serve -w 名字=路径` 可预置。
- **空工作区可降级**：缺少 `学习进度总览.md` 时不再报错，显示空态引导卡，可**一键初始化**（生成总览 + 岗位目标）；对话在未初始化时也能进行。
- CLI：`myblog init --workspace` 可同时初始化学习仓结构。
- **对话多会话**：每个会话**按名字建子文件夹**存储（`history/<工作区哈希>/<会话名>/messages.json`），默认名为 `新对话1 / 新对话2 …`；支持新建、切换、**重命名**（同步重命名文件夹）、删除；旧历史自动迁移。
- 设置 → 存储可修改对话历史目录；设置 → 功能精简说明文字。

### 修复

- **图标乱码**：把 `⌘ ◨ ⚙ ▤ ⓘ ✦ ✓ ↓` 等「字符当图标」全部换成内联 SVG，避免 Windows 字体回退导致乱码；spinner 盲文加字体回退。
- **下拉框不够圆润**：改用自绘 `Select` 组件（胶囊触发按钮 + 圆角浮层，替代原生 `<select>`，可用于工作区与对话服务商两项）。
- **对话页滚轮会把输入框顶走**：改为内部滚动（`.chat-body` 独立滚动 + `overscroll-behavior: contain`），输入框固定在底部。
- **折叠侧栏**：绿点不再消失（只隐藏文字）；校验红点缩小并加描边，不再盖住图标。
- **跟随系统图标**：改为中性底 + 半黑半白对比圆，消除两侧留白/劈裂。
- **强调色**：重调紫/绿/橙，蓝与紫不再相近。
- **切换深浅时窗口标题栏不同步**：桌面端通过 IPC 同步 `nativeTheme`，系统标题栏颜色随主题变化。
- **重命名会话没反应**：Electron 不支持 `window.prompt`，改为应用内对话框（重命名 + 删除确认）。
- **日期选择器突兀**：日期框统一圆角/边框/焦点环与主题图标，并加「今天」按钮。
- **窗口标题栏与主题/配色不协调**：改用自定义标题栏（隐藏原生标题栏 + `titleBarOverlay`），顶栏作为拖拽区，标题栏底色/文字色随主题同步更新。
- **窗口按钮颜色不合适**：改为**自绘窗口按钮**（`frame:false`），最小化/最大化/关闭由应用绘制，颜色随主题、关闭键 hover 变红，不再受系统纯黑符号影响。
- **弹框/菜单无动画**：遮罩淡入、对话框上浮缩放、下拉/日历浮层下滑淡入（遵循「减少动态效果」自动降级）。
- **每日总结日期选择**：换成自绘日历，可切换**年 / 月**、选择任意日期，并带「今天」按钮；日历浮层不再被列表裁剪（日期列表头移出滚动区）。
- 设置 → 功能：终端入口说明改为「显示内嵌终端页面，原生调用」。
- 设置页滚动时顶栏与窗口按钮不再滚走：**拆成两层布局**（固定 `header` + 独立滚动的 `content`），不再用吸顶/毛玻璃。
- 窗口按钮**恢复常显**（取消悬停隐藏）。
- 移除顶栏「刷新」按钮（保留命令面板的「刷新工作区」与实时同步）。
- 侧栏左下的阶段标签现在带上当前能力名（如 `G3 Hook`）。
- 设置：功能项说明去掉句号；主题/风格预览块改为固定宽度，不再占满整行。
- 设置：字号改为**拖动滑块**（小 / 中 / 大，刻度可点）。
- 内置对话新增工作区文件工具 `fs_list` / `fs_read` / `fs_write`：可读写工作区内的任意文本文件（脚本、代码等）；路径限制在工作区内，支持 `..` 越界拦截；写入默认 `dryRun=true` 先给 diff，用户确认后才落盘；已存在文件保留原行尾。

### 变更

- 强调色全面 token 化（`--accent-rgb`），边框/焦点环随强调色变化。
- **工作区切换移到侧栏「设置」上方**；「添加工作区」并入切换下拉菜单；下拉统一为自绘圆角组件（向上弹出）。
- **规划输入补全**：缺少「岗位目标.md」时，从总览「下次从哪继续」里的 G 编号推断当前阶段；`context` 与 `/api/today` 返回 `missing`（缺哪些输入）；今日卡片给出提示与「生成岗位目标」入口。
- **移除「浏览器打开」这条路径**：删除 CLI `myblog serve`（含 `--web / --api-only / --allow-remote / --token`），项目形态固定为**桌面应用 + CLI**；桌面端仍使用内嵌服务（内部 HTTP 为其加载界面的传输层）。README/AGENTS 同步更新。
- **CLI 瘦身**：只保留 `myblog status` / `myblog mcp` / `myblog init`，删除 `today` / `context` / `scaffold` / `close` / `check`（这些功能都在桌面端）；agent 接入模板与 README 改为以 MCP 工具为准。

## [1.1.2] - 2026-09-21

### 修复

- 对话框回复气泡四角圆角不统一：统一改用与卡片一致的 `--radius` 变量，去掉单独的 5px「小角」，助手气泡不再强制整行宽。
- 下拉框与应用风格不协调：设置里的「界面风格」改为与主题一致的**可视化预览块**；对话设置的下拉也统一为圆角 + 自绘箭头 + 一致焦点环。

## [1.1.1] - 2026-09-21

### 新增

- 设置 → 界面的**主题选择**改为可视化预览块（深色 / 浅色 / 跟随系统，各自渲染迷你界面预览）。
- **微交互**：列表（时间线、能力地图、日期列表）进入时**错落淡入**；按钮按压反馈；卡片 hover 提亮。
- **骨架屏**：概览数据加载中显示占位卡片，不再空白。

### 变更

- 支持系统「减少动态效果」偏好，动画自动降级。

## [1.1.0] - 2026-09-20

**UI 美化阶段（1.1.x）开始。**

### 新增

- 对话代码块：顶部**语言标签 + 复制按钮**。
- 助手消息：悬停显示**复制**按钮。
- 对话页：**回到底部**浮层按钮（向上翻看时出现）。
- 能力地图：由三段小方块改为**进度条**（未开始/进行中/已闭环三种配色）。

### 变更

- 支持系统「减少动态效果」偏好（`prefers-reduced-motion`）。
- 细节：代码块、气泡操作、复制按钮等视觉统一。

## [1.0.0] - 2026-09-20

**首个正式版。** 自本版起结束 0.x 测试阶段。

### 新增

- **自定义存储位置**（设置 → 存储）：
  - 「模型配置文件」路径可改（对话使用的 provider / key 的 `agent.json`）。
  - 「对话历史目录」路径可改（每个工作区一个 JSON）。
  - 桌面端每项带「浏览…」原生选文件夹，可「恢复默认值」。
  - 路径保存在工作区之外的 `settings.json`（Web 为 `~/.myblog/`，桌面为应用数据目录），重启保留、不进 git。

## [0.1.10] - 2026-09-19

### 修复

- 浅色主题下终端输入的命令颜色太浅看不清：补全 xterm 的 16 色 ANSI 调色板（浅色版把白/亮白映射为深色、亮色压暗），不再沿用为深色背景准备的默认配色。

## [0.1.9] - 2026-09-19

### 修复

- 浅色主题下终端仍为黑底：xterm 主题随明暗切换，且切换后即时更新。
- 浅色主题下代码块仍是深色：新增浅色版代码高亮配色，代码面改用主题色。
- 设置里的下拉框样式：去掉突兀的方形边框与原生箭头，改为圆角 + 自绘箭头。

### 变更

- 设置项文案去掉「（类 right.codes）」等临时说明。

## [0.1.8] - 2026-09-19

### 新增

- **界面风格**切换：`产品（类 right.codes）` / `原生（极简）`。产品风带渐变光晕、玻璃卡片、大标题与更宽松留白。
- **主题**切换：深色 / 浅色 / 跟随系统（`data-theme`）。
- 设置 → 界面新增「主题」「界面风格」两项。

### 变更

- 颜色全面 token 化，支持明暗主题；代码块在浅色主题下仍保持深色以便阅读高亮。

## [0.1.7] - 2026-09-19

### 新增

- 设置页拆成分类：**界面 / 功能 / 关于**。
- 全局偏好（本地保存）：动画效果、代码高亮、紧凑模式、内置对话、终端入口、实时同步。
- 关于页显示版本号、当前工作区路径与数据存放说明；`/api/status` 现在返回版本号。

## [0.1.6] - 2026-09-19

### 新增

- 侧栏「设置」入口 + 设置页。
- 「内置对话」开关：关闭后隐藏对话页且不加载模型配置与历史。

## [0.1.5] - 2026-09-19

### 新增

- 对话代码块**语法高亮**（rehype-highlight + 深色 token 主题）。
- 流式输出时自动补全未闭合的 ``` 围栏，代码边流边以代码块显示。

## [0.1.4] - 2026-09-19

### 变更

- 思考动画改为 opencode 同款**盲文 spinner**（`⠋⠙⠹…`，80ms 一帧）；工具执行中也用同一动画。
- 移除终端页的提示语。

## [0.1.3] - 2026-09-19

### 新增

- 工具调用改为单行卡片 + 折叠详情；写操作（写回/新建）高亮并默认展开 diff 预览。
- 新增 `tool_start` 事件，工具执行期间显示「执行中」。
- 聊天空状态快捷建议；气泡入场动效；模型状态点。

### 变更

- 系统提示：上下文已注入，不再重复调用 `myblog_context` / `myblog_check`。

## [0.1.2] - 2026-09-19

### 新增

- 对话设置里的**服务商预设**（OpenAI / DeepSeek / 通义 / Kimi / 智谱 / OpenRouter / Ollama），并显示配置文件路径。

### 修复

- 终端切换页面不再中断：视图首次打开后保持挂载，切回来自动重新适配尺寸。

## [0.1.1] - 2026-09-19

### 新增

- 桌面内置**真终端**（xterm.js + node-pty，winpty 后端），工作目录为当前学习库，可运行 opencode / claude / codex / 任意命令。

### 变更

- UI 升级：侧栏 SVG 图标、统一设计变量、卡片/按钮/焦点环、终端主题。

### 修复

- `node-pty` 原生编译：关闭 Spectre 要求并针对 Electron 重编（`npm run fix:native`）。
- ConPTY 辅助进程在 Electron 下崩溃 → 改用 winpty。

## [0.1.0] - 2026-09-19

### 首个版本

- `@myblog/core`：markdown 解析 / 生成 / 校验 / 组装；写回外科手术式、幂等、保持行尾。
- `@myblog/cli`：`status / today / context / scaffold / close / check / serve / init / mcp`。
- `@myblog/agent`：OpenAI 兼容模型客户端 + 工具循环（默认 dry-run 写回）。
- `@myblog/server`：Hono API、SSE 实时刷新、对话与历史接口。
- `@myblog/web`：Vite + React 仪表盘（概览 / 每日总结 / 对话 / 校验）。
- `@myblog/desktop`：Electron 薄壳，内置 server，无终端即可运行。
- 本地服务默认只绑 loopback；远程模式需 token。
- `.opencode / .claude / .cursor / AGENTS.md` 接入模板 + MCP。
